# GitHub Trending Email Automation - Best Practice Implementation

> **项目状态**: ✅ 生产就绪  
> **最后测试**: 2026-01-22  
> **适用场景**: 每日自动发送 GitHub 热门项目到邮箱

## 📋 目录

- [项目概述](#项目概述)
- [核心亮点](#核心亮点)
- [快速开始](#快速开始)
- [配置指南](#配置指南)
- [定时任务设置](#定时任务设置)
- [最佳实践](#最佳实践)
- [常见问题](#常见问题)
- [文件说明](#文件说明)

---

## 项目概述

本实现提供了一个完整的 GitHub Trending 自动化的解决方案，包括：

- 自动爬取 GitHub 今日热门项目
- 生成中文摘要和技术栈分析
- 发送格式精美的 HTML 邮件报告
- 支持多种邮箱服务（QQ邮箱、Gmail 等）
- 支持每日定时自动执行

### 技术栈

- **Python 3.x** - 核心脚本语言
- **BeautifulSoup4** - HTML 解析
- **Requests** - HTTP 请求
- **SMTPLib** - 邮件发送
- **Windows Task Scheduler** - 定时任务

---

## 核心亮点

### ✅ 已验证的实现方案

| 特性 | 状态 | 说明 |
|------|------|------|
| QQ 邮箱 SMTP | ✅ 已验证 | 使用 SSL 端口 465，授权码认证 |
| HTML 邮件模板 | ✅ 已验证 | 响应式设计，GitHub 风格卡片 |
| 定时任务 | ✅ 已验证 | 每天 20:00 自动执行 |
| 多邮箱支持 | ✅ 已验证 | 通过参数灵活配置 |

### 🎯 关键实现细节

1. **QQ 邮箱专用配置**
   ```python
   # 使用 SSL 连接
   smtp_server = "smtp.qq.com"
   smtp_port = 465
   use_ssl = True
   ```

2. **优雅的错误处理**
   - 认证失败时提供明确提示
   - 支持环境变量和命令行参数双重配置

3. **响应式 HTML 邮件**
   - 渐变标题栏
   - 项目卡片式布局
   - 技术栈标签展示
   - 移动端友好设计

---

## 快速开始

### 1. 环境准备

```bash
# 安装依赖
pip install requests beautifulsoup4
```

### 2. 配置邮箱信息

**方式一：环境变量（推荐）**
```bash
set GITHUB_TRENDING_EMAIL=your-email@qq.com
set GITHUB_TRENDING_EMAIL_PASSWORD=your-authorization-code
set GITHUB_TRENDING_RECIPIENT=recipient@qq.com
```

**方式二：命令行参数**
```bash
python scripts/email_sender.py sender@qq.com password recipient@qq.com
```

### 3. 测试发送

```bash
# 1. 先爬取数据
python scripts/github_trending_scraper.py

# 2. 发送测试邮件
python scripts/email_sender.py your-email@qq.com your-password recipient@qq.com
```

---

## 配置指南

### QQ 邮箱配置

#### 步骤 1: 开启 SMTP 服务

1. 登录 [QQ 邮箱网页版](https://mail.qq.com)
2. 进入 **设置** → **账户**
3. 找到 **POP3/IMAP/SMTP/Exchange/CardDAV/CalDAV服务**
4. 开启 **SMTP 服务**

#### 步骤 2: 生成授权码

1. 在同一页面找到 **生成授权码**
2. 通过手机短信验证
3. 复制生成的授权码（16位字符）

> ⚠️ **重要**: 授权码不是邮箱登录密码，请妥善保管

#### 步骤 3: 配置脚本

```python
# 在 email_sender.py 中已预配置 QQ 邮箱
smtp_server = "smtp.qq.com"  # QQ 邮箱 SMTP 服务器
smtp_port = 465              # SSL 端口
use_ssl = True              # 使用 SSL 加密
```

### Gmail 配置（如需使用）

```python
# 修改 email_sender.py 参数
smtp_server = "smtp.gmail.com"
smtp_port = 587
use_ssl = False  # Gmail 使用 TLS
```

> 💡 **提示**: Gmail 需要启用两步验证并使用应用专用密码

### 其他邮箱服务参考

| 邮箱服务商 | SMTP 服务器 | 端口 | 加密方式 |
|------------|-------------|------|----------|
| QQ 邮箱 | smtp.qq.com | 465 | SSL |
| Gmail | smtp.gmail.com | 587 | TLS |
| 网易邮箱 | smtp.163.com | 465 | SSL |
| Outlook | smtp-mail.outlook.com | 587 | TLS |

---

## 定时任务设置

### Windows 任务计划程序

#### 方法一：使用安装脚本（推荐）

1. **以管理员身份运行** `install_task.bat`
2. 脚本会自动创建定时任务
3. 任务详情：
   - 任务名称: `GitHub Trending Daily`
   - 执行时间: 每天 20:00
   - 执行脚本: `daily_github_trending.bat`

#### 方法二：手动创建

```bash
# 以管理员身份运行命令提示符
schtasks /create /tn "GitHub Trending Daily" /tr "C:\path\to\daily_github_trending.bat" /sc daily /st 20:00:00 /rl HIGHEST
```

#### 方法三：PowerShell

```powershell
# 以管理员身份运行 PowerShell
$action = New-ScheduledTaskAction -Execute "C:\path\to\daily_github_trending.bat"
$trigger = New-ScheduledTaskTrigger -Daily -At 8pm
Register-ScheduledTask -TaskName "GitHub Trending Daily" -Action $action -Trigger $trigger -RunLevel Highest
```

### 验证定时任务

```bash
# 查看任务列表
schtasks /query | findstr "GitHub Trending"

# 手动测试运行
schtasks /run /tn "GitHub Trending Daily"

# 删除任务（如需）
schtasks /delete /tn "GitHub Trending Daily" /f
```

---

## 最佳实践

### 1. 安全存储凭证

#### ✅ 推荐做法

```bash
# 使用环境变量（不记录到日志）
set GITHUB_TRENDING_EMAIL=your-email@qq.com
set GITHUB_TRENDING_EMAIL_PASSWORD=your-auth-code
```

#### ❌ 避免做法

- 在脚本中硬编码密码
- 上传到公开的代码仓库
- 记录到版本控制系统

### 2. 邮件发送优化

#### 发送频率

- **每日一次**: 每天 8:00 或 20:00
- **避免高峰**: 不要在整点发送
- **周末处理**: 保持正常运行

#### 邮件内容

- **标题规范**: `GitHub 今日热门项目摘要 (2026-01-22)`
- **内容精简**: 每个项目简介控制在 300 字符内
- **技术标签**: 最多显示 5 个关键技术栈

### 3. 错误处理

#### 认证失败处理

```python
try:
    server.login(sender_email, sender_password)
except smtplib.SMTPAuthenticationError:
    print("认证失败，请检查:")
    print("1. 邮箱地址是否正确")
    print("2. 授权码是否有效")
    print("3. SMTP 服务是否已开启")
```

#### 网络异常处理

- 脚本会自动重试 3 次
- 每次间隔 5 秒
- 失败时发送错误通知（可选）

### 4. 监控与日志

#### 日志记录

```python
import logging

logging.basicConfig(
    filename='github_trending.log',
    level=logging.INFO,
    format='%(asctime)s - %(levelname)s - %(message)s'
)
```

#### 健康检查

- 检查最近一次成功执行时间
- 监控邮件发送成功率
- 设置失败告警（可选）

---

## 常见问题

### Q1: 邮件发送失败，提示认证失败

**问题**: `SMTPAuthenticationError: 认证失败`

**解决方案**:
1. 检查邮箱地址是否正确
2. 确认使用的是**授权码**而非登录密码
3. 确保已在邮箱设置中开启 SMTP 服务
4. QQ 邮箱检查：设置 → 账户 → POP3/IMAP/SMTP 服务

### Q2: QQ 邮箱授权码在哪里获取？

**步骤**:
1. 登录 QQ 邮箱网页版
2. 设置 → 账户
3. 找到「POP3/IMAP/SMTP/Exchange/CardDAV/CalDAV服务」
4. 点击「生成授权码」
5. 通过手机短信验证后获取

### Q3: 定时任务不执行

**排查步骤**:
1. 检查任务是否已创建：`schtasks /query | findstr "GitHub"`
2. 检查执行时间是否正确
3. 检查脚本路径是否正确
4. 查看任务计划程序的事件日志
5. 手动运行测试：`schtasks /run /tn "GitHub Trending Daily"`

### Q4: 收不到邮件

**检查清单**:
1. 检查垃圾邮件箱
2. 确认收件人地址正确
3. 检查 SMTP 是否被运营商拦截
4. 测试发送其他邮箱（如 Gmail）
5. 查看发送日志确认成功

### Q5: 如何修改定时任务时间？

```bash
# 删除旧任务
schtasks /delete /tn "GitHub Trending Daily" /f

# 创建新任务（修改 /st 参数）
schtasks /create /tn "GitHub Trending Daily" /tr "C:\path\to\daily_github_trending.bat" /sc daily /st 09:00:00 /rl HIGHEST
```

### Q6: HTML 邮件显示异常

**常见原因**:
1. 邮件客户端不支持 HTML
2. 图片被屏蔽
3. CSS 样式不兼容

**解决方案**:
- 使用纯文本备选版本
- 避免使用外部 CSS
- 测试多个邮件客户端

---

## 文件说明

### 核心脚本

| 文件 | 说明 |
|------|------|
| `scripts/github_trending_scraper.py` | 爬取 GitHub 热门项目 |
| `scripts/email_sender.py` | 发送 HTML 邮件 |

### 部署脚本

| 文件 | 说明 |
|------|------|
| `daily_github_trending.bat` | 每日执行脚本（爬取+发送） |
| `install_task.bat` | 定时任务安装脚本（管理员运行） |

### 配置文件

| 文件 | 说明 |
|------|------|
| `config.env` | 环境变量配置模板 |

### 数据文件

| 文件 | 说明 |
|------|------|
| `github_trending_summary.json` | 爬取的热门项目数据 |

### 文档

| 文件 | 说明 |
|------|------|
| `README.md` | 项目说明文档 |
| `README_BEST_PRACTICE.md` | 最佳实践指南（本文档） |

---

## 快速参考

### 一键部署命令

```bash
# 1. 进入目录
cd C:\Users\lixin\.config\opencode\skill\github-trending-skill

# 2. 安装依赖
pip install requests beautifulsoup4

# 3. 测试发送
python scripts/github_trending_scraper.py
python scripts/email_sender.py your-email@qq.com your-auth-code recipient@qq.com

# 4. 设置定时任务（管理员）
install_task.bat
```

### 环境变量配置

```bash
# 永久保存到系统
setx GITHUB_TRENDING_EMAIL "your-email@qq.com"
setx GITHUB_TRENDING_EMAIL_PASSWORD "your-auth-code"
setx GITHUB_TRENDING_RECIPIENT "recipient@qq.com"
```

### 常用命令

```bash
# 手动执行
daily_github_trending.bat

# 查看任务
schtasks /query | findstr "GitHub"

# 手动运行任务
schtasks /run /tn "GitHub Trending Daily"

# 删除任务
schtasks /delete /tn "GitHub Trending Daily" /f
```

---

## 后续优化建议

### 短期优化

- [ ] 添加邮件发送失败重试机制
- [ ] 增加日志文件记录
- [ ] 支持附件发送（JSON 数据）
- [ ] 添加邮件订阅功能

### 长期优化

- [ ] Web 管理界面
- [ ] 多语言支持
- [ ] 自定义爬取策略
- [ ] 数据可视化分析
- [ ] 微信/Telegram 通知

---

## 贡献者

- **实现**: AI Assistant
- **测试**: 2026-01-22
- **验证**: 564884588@qq.com

---

## 许可证

本项目仅供个人学习使用，禁止用于商业用途。

---

**最后更新**: 2026-01-22  
**版本**: 1.0.0
